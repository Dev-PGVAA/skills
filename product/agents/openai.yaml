interface:
  display_name: "Product"
  short_description: "Evaluate, plan, and deliver the authorized product"
  default_prompt: "Use $product to advance this idea or implementation request from its current stage to a verified outcome."
