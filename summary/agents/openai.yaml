interface:
  display_name: "Summary"
  short_description: "Faithful summaries, evidence, and action items"
  default_prompt: "Use $summary to condense these sources while preserving attribution, uncertainty, and commitments."
